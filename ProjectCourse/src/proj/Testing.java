package proj;

public class Testing {
	/*
	public static void main(String[] args){
		
		MyArrayList list = new MyArrayList();
		
		ShapeFactory factory = new ShapeFactory();
		
		list.append(factory.getShape("RECTANGLE"));
		list.append(factory.getShape("CIRCLE"));
		list.append(factory.getShape("CIRCLE"));
		list.append(factory.getShape("SQUARE"));
		list.append(factory.getShape("RECTANGLE"));
		list.append(factory.getShape("RECTANGLE"));
		list.append(factory.getShape("CIRCLE"));
		
		
		for(int i = 0; i < list.getSize(); i++){
			if(list.get(i) instanceof Circle){
				System.out.println(list.get(i).toString());
			}
		}
		
		
		BigCircleIterator cit = list.createBigCircIterator();
		
		for (cit.firstCircle();!(cit.endList()); cit.nextCircle()){
			System.out.println(cit.currentCircle().toString());
		}
		
		RectSquaresIterator rit = list.createRectSquareIterator();
		
		for (rit.firstRect();!(rit.endList()); rit.nextRect()){
			System.out.println(rit.currentRect().toString());
		}
	}*/
}
